import 'package:flutter/widgets.dart';

class UiForm extends StatelessWidget {
  final Widget child;

  const UiForm({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) => child;
}

