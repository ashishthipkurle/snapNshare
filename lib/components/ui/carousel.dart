import 'package:flutter/material.dart';

class UiCarousel extends StatelessWidget {
  const UiCarousel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => SizedBox(height: 140, child: Center(child: Text('Carousel placeholder')));
}

