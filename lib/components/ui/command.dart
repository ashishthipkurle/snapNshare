import 'package:flutter/material.dart';

class UiCommand extends StatelessWidget {
  final Widget child;

  const UiCommand({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) => child;
}

