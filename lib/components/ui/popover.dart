import 'package:flutter/material.dart';

class UiPopover extends StatelessWidget {
  final Widget child;

  const UiPopover({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) => child;
}

