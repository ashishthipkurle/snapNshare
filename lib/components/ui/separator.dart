import 'package:flutter/material.dart';

class UiSeparator extends StatelessWidget {
  const UiSeparator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Divider();
}

