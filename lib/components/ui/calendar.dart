import 'package:flutter/material.dart';

class UiCalendar extends StatelessWidget {
  const UiCalendar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container(height: 200, color: Colors.transparent, child: Center(child: Text('Calendar placeholder')));
}

