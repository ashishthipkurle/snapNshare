import 'package:flutter/widgets.dart';

class Resizable extends StatelessWidget {
  final Widget child;

  const Resizable({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) => child;
}

